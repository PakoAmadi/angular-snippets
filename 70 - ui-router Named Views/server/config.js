var settings = {
    port: 3444,
    data_location: '../data',
    rest_base_url: '/api/*', //what is this?
    static_site_root: __dirname + '/../build' //up a dir and find build
};

module.exports = settings;