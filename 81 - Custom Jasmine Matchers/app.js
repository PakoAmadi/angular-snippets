var app = angular.module('app', []);

app.controller('StatusCtrl',
    ['$scope',
        function ($scope) {

            this.levelUp = function() {

            };

            $scope.levelDown = function() {

            };

        }]);